export type CurrencyItem = {
    full_name: string,
    id: number,
    name: string
};

export type UpdateCurrencyDates = {
    currency: number
}

export type UpdateDailyAmountDates = {
    daily_amount: number
}
